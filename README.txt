This project demonstrates the java reflections api.

The input file has serializable objects which are in a irregular order.
These objects from file are initially read from the file and then are serialized and deserialized 
using strategy pattern.

THe type of objects are classified using reflections api